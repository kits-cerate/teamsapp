import React from "react";
import { Calendar } from "@material-ui/pickers";

const CalendarView = () => {
  return (
    <div>
      <Calendar></Calendar>
    </div>
  );
};

export default CalendarView;
